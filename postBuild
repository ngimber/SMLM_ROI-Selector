#!/bin/bash
echo "SMLM ROI Selector Binder environment ready!"
echo "To run the application, execute: python smlm_roi_selector.py"
echo "Note: The tkinter GUI will launch in your terminal environment."
